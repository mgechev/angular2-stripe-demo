import { RouterConfig } from '@angular/router';

import { CustomFormComponent } from './index';

export const CustomFormRoutes: RouterConfig = [
  {
    path: 'custom-form',
    component: CustomFormComponent
  },
];
